# Configuration de la base de données MySQL (WAMP)
# Copiez ce fichier vers .env et modifiez les valeurs selon votre configuration

# Base de données MySQL
DB_NAME=omapi_database
DB_USER=omapi_user
DB_PASSWORD=omapi_password
DB_HOST=localhost
DB_PORT=3306

# Configuration Django
SECRET_KEY=django-insecure-&^ylp*ibs!8+c_e0coqa_odq!^ffzzroax*q1h2i-jk*4cf+e(
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# Configuration email
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend
DEFAULT_FROM_EMAIL=daniel.razanajaona@gmail.com

# Configuration MySQL spécifique
MYSQL_CHARSET=utf8mb4
MYSQL_COLLATION=utf8mb4_unicode_ci